ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment'
require 'sinatra/activerecord/rake' #to get rake tasks from sinatra_active record gem 


task :console do 
    Pry.start
end